{
  "name": "homura-example-webapp",
  "version": "0.1.0",
  "description": "Example webapp using Homura - Ruby DSL for Cloudflare Workers",
  "type": "module",
  "scripts": {
    "bundle:ruby": "node scripts/bundle-ruby.js",
    "build:mruby": "cd ../../mruby && make",
    "build": "npm run bundle:ruby && npm run build:mruby",
    "dev": "npm run bundle:ruby && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  },
  "license": "MIT"
}
